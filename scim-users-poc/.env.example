# Bearer token the IdP's SCIM client must present on every request.
# Generate one:  openssl rand -hex 24
# Paste the SAME value into Entra: Enterprise App -> Provisioning -> Secret Token.
SCIM_TOKEN=

# Port for the SCIM server + UI API (ngrok points here).
PORT=5175
